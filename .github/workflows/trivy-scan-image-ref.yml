name: Trivy Scan Docker Image
permissions:
  id-token: write
  contents: read
on:
  workflow_call:
    inputs:
      image-ref:
        description: "Full Docker image reference to scan"
        required: true
        type: string
      environment:
        description: "GitHub environment to use (for secrets/vars)"
        required: true
        type: string
      severity:
        description: "Comma-separated severities to fail on"
        required: false
        default: "CRITICAL,HIGH"
        type: string
      exit-code:
        description: "Exit code when vulnerabilities are found"
        required: false
        default: "1"
        type: string
      release_bucket_name:
        description: "GCS bucket name for artifacts"
        required: true
        type: string
      release_bucket_prefix:
        description: "GCS bucket prefix (usually repository)"
        required: false
        type: string
        default: ${{ github.repository }}
      version:
        description: "Release version (used as GCS path)"
        required: true
        type: string

    secrets:
      GOOGLE_CLOUD_PROJECT_ID:
        required: true
      GOOGLE_CLOUD_SA_KEY_GITHUB_ACTIONS_MAIN:
        required: true
      WORKLOAD_IDENTITY_PROVIDER:
        required: true

jobs:
  trivy-scan:
    runs-on: self-hosted
    environment: ${{ inputs.environment }}
    name: Trivy Scan (${{ inputs.environment }})
    env:
      WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
      GOOGLE_CLOUD_SA_KEY_GITHUB_ACTIONS_MAIN: ${{ secrets.GOOGLE_CLOUD_SA_KEY_GITHUB_ACTIONS_MAIN }}
      GOOGLE_CLOUD_PROJECT_ID: ${{ secrets.GOOGLE_CLOUD_PROJECT_ID }}
      IMAGE_REF: europe-west1-docker.pkg.dev/${{ secrets.GOOGLE_CLOUD_PROJECT_ID }}/${{ inputs.image-ref }}
      VEGITO_RELEASE_BUCKET: ${{ inputs.release_bucket_name }}/${{ inputs.release_bucket_prefix }}
      VERSION: ${{ inputs.version }}
      INFRA_ENV: ${{ inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GOOGLE_CLOUD_SA_KEY_GITHUB_ACTIONS_MAIN }}
          project_id: ${{ env.GOOGLE_CLOUD_PROJECT_ID }}
          token_format: access_token
          export_environment_variables: true

      - name: Configure Cloud SDK and Docker
        run: |
          make gcloud-config-set-project
          make gcloud-auth-docker

      - name: Run Trivy scan (HTML report)
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: ${{ env.IMAGE_REF }}
          format: template
          template: "@.github/templates/trivy-html.tpl.html"
          output: trivy-report.html
          exit-code: ${{ inputs.exit-code }}
          ignore-unfixed: true
          vuln-type: os,library
          severity: ${{ inputs.severity }}
          version: v0.69.3

      - name: Upload Trivy HTML report to GCS
        if: always()
        run: |
          DEST="gs://${VEGITO_RELEASE_BUCKET}/${VERSION}/security/trivy-report.html"
          echo "Uploading trivy-report.html to $DEST"
          gcloud storage cp \
            --content-type=text/html \
            trivy-report.html \
            "$DEST"

      - name: Publish security metadata
        run: |
          set -euo pipefail

          BASE="gs://${VEGITO_RELEASE_BUCKET}/${VERSION}"

          REPORT="https://storage.googleapis.com/${VEGITO_RELEASE_BUCKET}/${VERSION}/security/trivy-report.html"

          cat <<EOF > security-metadata.json
          {
            "securityReport": "${REPORT}"
          }
          EOF

          gcloud storage cp security-metadata.json ${BASE}/security-metadata.json
